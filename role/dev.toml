# role/dev — developer workstation
#
# Containers, language toolchains, editors, debugging tools.
# Pairs with any platform and desktop.

[fragment]
schema      = "io.styrene.nex.profile-fragment.v1"
id          = "role/dev"
name        = "dev"
version     = "0.1.0"
description = "Developer workstation — containers, editors, language tools, debugging"
category    = "role"
platforms   = ["linux"]
visibility  = "public"

[packages]
nix = [
  "docker",
  "docker-compose",
  "kubectl",
  "k9s",
  "tmux",
  "gh",
  "direnv",
  "shellcheck",
  "nixfmt-rfc-style",
  "strace",
  "file",
  "openssl",
]

[shell.aliases]
k   = "kubectl"
dc  = "docker compose"
g   = "git"
gs  = "git status"
gd  = "git diff"
gl  = "git log --oneline -20"
